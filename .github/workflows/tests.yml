name: tests

on:
  pull_request:
  push:
    branches:
      - master

jobs:
  build-run-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: yarn install --frozen-lockfile
      - run: yarn turbo run build
      - run: yarn turbo run test-affected-packages
